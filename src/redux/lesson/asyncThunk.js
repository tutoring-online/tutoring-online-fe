import { createAsyncThunk } from "@reduxjs/toolkit"
import * as types from "./types";
// import lesson
const fetchAllLessons = createAsyncThunk(
    types.FETCH_ALL_LESSON,
    async () => {
        // const reponse = a
    }
) 